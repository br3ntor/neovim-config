require("diffview").setup()
--require("diffview").setup({
--  use_icons = true,
--  icons = { -- Only applies when use_icons is true.
--    folder_closed = "",
--    folder_open = "",
--  },
--
--
--})
